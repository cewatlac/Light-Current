# Generation Workflow

1. Parse the CSV.
2. Classify topic importance.
3. Generate data indexes.
4. Generate all topic pages and top-level pages.
5. Build the internal linking dictionary.
6. Apply internal links.
7. Validate pages, links, and content rules.
8. Generate reports.

Use `npm.cmd run build` on this machine.
